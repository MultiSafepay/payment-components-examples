/*
This is the config file to choose the server to be run
type: set value to PHP or NODEJS according the one you need to run
apiEndpoint: set the url to conect with the desired enviroments, possible options are:
"https://api-10.dev.multisafepay.com/v1/",
"https://api.multisafepay.com/v1/connect/"
*/
const serverSettings = {
    type: 'NODEJS',
    apiEndpoint: "https://api-10.dev.multisafepay.com/v1/"   
}